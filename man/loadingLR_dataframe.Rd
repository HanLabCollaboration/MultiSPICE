% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading_dataframe.R
\name{loadingLR_dataframe}
\alias{loadingLR_dataframe}
\title{loadingLR_dataframe}
\usage{
loadingLR_dataframe(
  data,
  sample_col = NULL,
  source_col = "source",
  target_col = "target",
  ligand_col = "ligand",
  receptor_col = "receptor",
  interaction_col = NULL,
  value_col = "pval",
  value_type = "pval"
)
}
\arguments{
\item{data}{A dataframe or list of dataframes containing LR interaction data}

\item{sample_col}{Name of column containing sample IDs (required if data is a single dataframe)}

\item{source_col}{Name of column containing source/sender cell type (default: "source")}

\item{target_col}{Name of column containing target/receiver cell type (default: "target")}

\item{ligand_col}{Name of column containing ligand gene (default: "ligand")}

\item{receptor_col}{Name of column containing receptor gene (default: "receptor")}

\item{interaction_col}{Name of column containing interaction name (e.g., "LIGAND_RECEPTOR").
If NULL, will be created from ligand and receptor columns (default: NULL)}

\item{value_col}{Name of column containing significance values (default: "pval")}

\item{value_type}{Type of value in value_col: "pval" or "prob" (default: "pval")}
}
\value{
A named list where each element is a dataframe for one sample with columns:
  source, target, ligand, receptor, interaction_name, and pval/prob
}
\description{
Load ligand-receptor pair data from user-provided dataframes
}
\examples{
\dontrun{
# Example 1: Single dataframe with sample column
df <- data.frame(
  sample = c("S1", "S1", "S2", "S2"),
  source = c("T cell", "B cell", "T cell", "B cell"),
  target = c("Tumor", "Tumor", "Tumor", "Tumor"),
  ligand = c("IFNG", "TNF", "IFNG", "TNF"),
  receptor = c("IFNGR1", "TNFRSF1A", "IFNGR1", "TNFRSF1A"),
  pval = c(0.001, 0.01, 0.002, 0.015)
)
lr_list <- loadingLR_dataframe(df, sample_col = "sample")

# Example 2: List of dataframes (one per sample)
df_list <- list(
  Sample1 = data.frame(
    source = c("T cell", "B cell"),
    target = c("Tumor", "Tumor"),
    ligand = c("IFNG", "TNF"),
    receptor = c("IFNGR1", "TNFRSF1A"),
    pval = c(0.001, 0.01)
  ),
  Sample2 = data.frame(
    source = c("T cell", "B cell"),
    target = c("Tumor", "Tumor"),
    ligand = c("IFNG", "TNF"),
    receptor = c("IFNGR1", "TNFRSF1A"),
    pval = c(0.002, 0.015)
  )
)
lr_list <- loadingLR_dataframe(df_list)
}
}
