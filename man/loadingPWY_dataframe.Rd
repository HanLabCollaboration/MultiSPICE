% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loading_dataframe.R
\name{loadingPWY_dataframe}
\alias{loadingPWY_dataframe}
\title{loadingPWY_dataframe}
\usage{
loadingPWY_dataframe(
  data,
  sample_col = NULL,
  source_col = "source",
  target_col = "target",
  pathway_col = "pathway",
  value_col = "pval",
  value_type = "pval"
)
}
\arguments{
\item{data}{A dataframe or list of dataframes containing pathway interaction data}

\item{sample_col}{Name of column containing sample IDs (required if data is a single dataframe)}

\item{source_col}{Name of column containing source/sender cell type (default: "source")}

\item{target_col}{Name of column containing target/receiver cell type (default: "target")}

\item{pathway_col}{Name of column containing pathway name (default: "pathway")}

\item{value_col}{Name of column containing significance values (default: "pval")}

\item{value_type}{Type of value in value_col: "pval" or "prob" (default: "pval")}
}
\value{
A named list where each element is a dataframe for one sample with columns:
  source, target, pathway, and pval/prob
}
\description{
Load pathway-level interaction data from user-provided dataframes
}
\examples{
\dontrun{
# Example 1: Single dataframe with sample column
df <- data.frame(
  sample = c("S1", "S1", "S2", "S2"),
  source = c("T cell", "B cell", "T cell", "B cell"),
  target = c("Tumor", "Tumor", "Tumor", "Tumor"),
  pathway = c("IFN-II", "TNF", "IFN-II", "TNF"),
  pval = c(0.001, 0.01, 0.002, 0.015)
)
pwy_list <- loadingPWY_dataframe(df, sample_col = "sample")

# Example 2: List of dataframes (one per sample)
df_list <- list(
  Sample1 = data.frame(
    source = c("T cell", "B cell"),
    target = c("Tumor", "Tumor"),
    pathway = c("IFN-II", "TNF"),
    pval = c(0.001, 0.01)
  ),
  Sample2 = data.frame(
    source = c("T cell", "B cell"),
    target = c("Tumor", "Tumor"),
    pathway = c("IFN-II", "TNF"),
    pval = c(0.002, 0.015)
  )
)
pwy_list <- loadingPWY_dataframe(df_list)
}
}
