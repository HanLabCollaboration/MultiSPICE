% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_boxplot.R
\name{feature_boxplot}
\alias{feature_boxplot}
\title{Create Boxplot for Individual Features}
\usage{
feature_boxplot(
  matrix,
  metadata,
  features = NULL,
  group_col = "Group",
  colors = NULL,
  test_method = "wilcox_test",
  p_adjust_method = "none",
  hide_ns = FALSE,
  ncol = 2,
  point_size = 2,
  title_size = 15,
  axis_title_size = 14,
  axis_text_size = 12,
  legend_text_size = 14,
  show_x_axis = FALSE,
  y_label = "Interaction Counts"
)
}
\arguments{
\item{matrix}{A feature matrix with features as rows and samples as columns
(output from LR_matrix or PWY_matrix)}

\item{metadata}{Data frame containing sample metadata with group information.
Must have a 'sample' column matching matrix column names.}

\item{features}{Character vector of feature names to plot. If NULL, plots all features (default: NULL)}

\item{group_col}{Name of the column in metadata containing group labels (default: "Group")}

\item{colors}{Named vector of colors for groups. If NULL, uses default colors (default: NULL)}

\item{test_method}{Statistical test method: "wilcox_test", "t_test", or "anova" (default: "wilcox_test")}

\item{p_adjust_method}{P-value adjustment method: "none", "bonferroni", "BH", etc. (default: "none")}

\item{hide_ns}{Logical, whether to hide non-significant comparisons (default: FALSE)}

\item{ncol}{Number of columns for faceting when plotting multiple features (default: 2)}

\item{point_size}{Size of jittered points (default: 2)}

\item{title_size}{Font size for plot titles (default: 15)}

\item{axis_title_size}{Font size for axis titles (default: 14)}

\item{axis_text_size}{Font size for axis text (default: 12)}

\item{legend_text_size}{Font size for legend text (default: 14)}

\item{show_x_axis}{Logical, whether to show x-axis labels (default: FALSE)}

\item{y_label}{Label for y-axis (default: "Interaction Counts")}
}
\value{
A ggplot object or list of ggplot objects (if multiple features)
}
\description{
Creates boxplots comparing feature counts (LR pairs or pathways) across groups.
Includes statistical testing and customizable aesthetics.
}
\examples{
\dontrun{
# Create LR feature matrix
lr_matrix <- LR_matrix(lr_list = LR_pairs_list, rank_table = rank_table, 
                       cutoff = 0.01, value = "pval")

# Create metadata
metadata <- data.frame(
  sample = colnames(lr_matrix),
  Group = c("Control", "Treatment", "Control", "Treatment")
)

# Plot single feature
feature_boxplot(lr_matrix, metadata, features = "CXCL12_CXCR4")

# Plot multiple features
features <- c("CXCL12_CXCR4", "VEGFA_VEGFR2", "TNF_TNFRSF1A")
feature_boxplot(lr_matrix, metadata, features = features, ncol = 3)

# Plot with custom colors
colors <- c(Control = "blue", Treatment = "red")
feature_boxplot(lr_matrix, metadata, features = "CXCL12_CXCR4", colors = colors)
}

}
