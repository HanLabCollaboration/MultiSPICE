% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heterogeneity.R
\name{plot_lr_mean_jaccard_boxplot}
\alias{plot_lr_mean_jaccard_boxplot}
\title{Boxplot of mean pairwise Jaccard per sample}
\usage{
plot_lr_mean_jaccard_boxplot(
  diss,
  title = NULL,
  label_samples = TRUE,
  box_fill = "#B8D4F0"
)
}
\arguments{
\item{diss}{List returned by \code{sample_lr_dissimilarity()} (must contain
a square \code{jaccard} matrix with at least two samples).}

\item{title}{Plot title.}

\item{label_samples}{If TRUE (default), draw sample names with \code{geom_text}
at the same jitter positions as the points. If FALSE, jittered points are
still drawn; only the text labels are omitted.}

\item{box_fill}{Fill color for the box (default \code{"#B8D4F0"}, light blue).}
}
\value{
A \code{ggplot2} object.
}
\description{
For each sample, computes the mean of off-diagonal \code{jaccard} entries from
\code{\link{sample_lr_dissimilarity}} (same summary as the former bar chart).
Plots a single cohort-level boxplot of those means, with jittered points and
optional sample labels to show each sample's value.
}
\seealso{
\code{\link{sample_lr_dissimilarity}}, \code{\link{plot_lr_sample_comparison}}
}
