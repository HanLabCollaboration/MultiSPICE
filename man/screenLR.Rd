% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screening.R
\name{screenLR}
\alias{screenLR}
\title{screenLR}
\usage{
screenLR(
  lr_list,
  type = "rank",
  threshold = 0.8,
  cutoff = 0.01,
  value = "pval",
  all_pairs = FALSE
)
}
\arguments{
\item{lr_list}{A list of data frames from loadingLR_cellchat, where each element is a sample
with columns: source, target, ligand, receptor, interaction_name, and pval/prob}

\item{type}{Screening method, currently only "rank" is supported.}

\item{threshold}{Proportion of samples in which a ligand-receptor pair must be significant 
to be retained (default is 0.8).}

\item{cutoff}{Significance cutoff threshold (default is 0.01). For pval: values < cutoff are significant. For prob: values > cutoff are significant.}

\item{value}{Type of values in the input, "pval" or "prob"}

\item{all_pairs}{Logical, if TRUE returns all pairs with their ranks, if FALSE returns only 
pairs passing the threshold.}
}
\value{
A data frame of screened ligand-receptor pairs with columns:
  source, target, ligand, receptor, interaction_name, rank
}
\description{
Screening ligand-receptor pairs based on significance across multiple samples.
}
\examples{
\dontrun{
# Load LR data from CellChat objects
lr_data <- loadingLR_cellchat(sample = samples, directory = dirs, value = "pval")
# Screen for consistent LR pairs
screened <- screenLR(lr_data, threshold = 0.8, cutoff = 0.01)
}
}
